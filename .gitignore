.DS_Store
*.log

# Runtime dirs (created on live server) — keep deny rules tracked
/cache/*
!/cache/.htaccess
!/cache/.gitkeep
/logs/

# Local / secret config — never commit
includes/.auth-secret
includes/.cred-master.key
install.lock
.setup.lock
.schema.lock
.admin-schema.lock
.member-schema.lock
includes/database.local.php
includes/database.php
*.local.php
# Member bulk-import CSV uploads (keep deny rules)
assets/uploads/member-imports/*
!assets/uploads/member-imports/.htaccess
!assets/uploads/member-imports/.gitkeep
