# File cache JSON must never be HTTP-readable (md5 keys are guessable).
# Apache 2.2 + 2.4 compatible.

Options -Indexes

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
