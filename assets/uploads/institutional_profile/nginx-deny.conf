# Nginx: deny direct access to institutional profile uploads.
# Include inside the server { } block, or paste the location block.
# Apache uses .htaccess in this folder.
# Files must be served only via /institutional-profile-file.php.

location ^~ /assets/uploads/institutional_profile/ {
    deny all;
    return 403;
}
