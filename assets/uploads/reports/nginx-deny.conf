# Nginx: deny direct access to report uploads.
# Include inside the server { } block, or paste the location block.
# Apache uses .htaccess in this folder.
# Files must be served only via /report-file.php (member gate + public proxy).

location ^~ /assets/uploads/reports/ {
    deny all;
    return 403;
}
