Options -Indexes
# Reports served only via /report-file.php (member gate + public proxy)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
