# Deny direct web access to PHP sources and secrets under /includes/
# (config, crypto keys, database credentials must never be HTTP-readable)

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
