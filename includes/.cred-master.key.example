# Office credential vault master key — per install only.
# Do NOT commit a real key. Copy is not needed: config.php auto-creates
# includes/.cred-master.key on first request if missing.
# Keep that file on the server (chmod 600). Never share across projects.
